name: release

on:
  push:
    branches: [ release ]
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: touch sftp_test.txt
      - uses: wangyucode/sftp-upload-action@v1.1
        with:
          # 
          host: ${{ secrets.DEPLOY_HOST }}
          # 
          password: ${{ secrets.DEPLOY_PASS}}
          # 
          localDir: ./sftp_test.txt
          # 
          remoteDir: ~/sftp_test.txt
